name: Build Mihomo

on:
  workflow_dispatch:
    inputs:
      version:
        description: "Mihomo version"
        required: true
        default: "v1.19.24"

jobs:
  build:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    strategy:
      fail-fast: false

      matrix:
        include:
          - goos: linux
            goarch: amd64
            artifact: mihomo-linux-amd64

          - goos: linux
            goarch: arm64
            artifact: mihomo-linux-arm64

          - goos: linux
            goarch: arm
            goarm: 7
            artifact: mihomo-linux-armv7

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.26'
          check-latest: true
          cache: true

      - name: Show Go Version
        run: go version

      - name: Build
        run: |
          chmod +x build.sh

          GOOS=${{ matrix.goos }} \
          GOARCH=${{ matrix.goarch }} \
          GOARM=${{ matrix.goarm }} \
          ./build.sh "${{ github.event.inputs.version }}"

          mv build/output/mihomo \
            build/output/${{ matrix.artifact }}

      - name: Generate SHA256
        run: |
          cd build/output
          sha256sum ${{ matrix.artifact }} > ${{ matrix.artifact }}.sha256

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: |
            build/output/${{ matrix.artifact }}
            build/output/${{ matrix.artifact }}.sha256

  release:
    needs: build
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: Download Artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Show Files
        run: find artifacts -type f

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.event.inputs.version }}
          name: Mihomo ${{ github.event.inputs.version }}
          generate_release_notes: true
          files: artifacts/**/*